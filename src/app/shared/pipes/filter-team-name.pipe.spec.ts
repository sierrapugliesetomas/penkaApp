import { FilterTeamNamePipe } from './filter-team-name.pipe';

describe('FilterTeamNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeamNamePipe();
    expect(pipe).toBeTruthy();
  });
});
