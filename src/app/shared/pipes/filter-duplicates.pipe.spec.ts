import { FilterDuplicatesPipe } from './filter-duplicates.pipe';

describe('FilterDuplicatesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDuplicatesPipe();
    expect(pipe).toBeTruthy();
  });
});
