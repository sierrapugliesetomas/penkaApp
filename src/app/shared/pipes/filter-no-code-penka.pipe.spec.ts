import { FilterNoCodePenkaPipe } from './filter-no-code-penka.pipe';

describe('FilterNoCodePenkaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNoCodePenkaPipe();
    expect(pipe).toBeTruthy();
  });
});
