import {FilterTempPipe} from './filter-temp.pipe';

describe('FilterTempPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterTempPipe();
        expect(pipe).toBeTruthy();
    });
});
