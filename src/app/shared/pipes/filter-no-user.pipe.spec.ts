import { FilterNoUserPipe } from './filter-no-user.pipe';

describe('FilterNoUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNoUserPipe();
    expect(pipe).toBeTruthy();
  });
});
